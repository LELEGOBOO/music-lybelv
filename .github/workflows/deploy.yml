name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SUPABASE_URL: ${{ secrets.PROJECT_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SERVICE_ROLE_KEY }}
      SUPABASE_ANON_KEY: ${{ secrets.ANON_KEY }}

    steps:
      - uses: actions/checkout@v3

      - name: Install Supabase CLI
        run: |
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt-get install -y nodejs
          npm install -g supabase

      - name: Deploy functions
        run: |
          supabase login --service-role $SUPABASE_SERVICE_ROLE_KEY
          supabase functions deploy contact --project-ref $SUPABASE_URL
